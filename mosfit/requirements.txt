# mpi4py 4.x provides wheels for modern Python versions; an MPI runtime is
# still required when actually launching MOSFiT under MPI.
#
# NumPy 2 is the supported baseline. Avoid mixing NumPy 1.x packages with this environment.
#
# If ~/.local shadows the conda env (older astropy vs env NumPy), reactivate after:
#   conda env config vars set PYTHONNOUSERSITE=1 -n <env>
# or prefix runs with PYTHONNOUSERSITE=1.
setuptools
astrocats>=0.3.33
astropy>=5.0.1
Cython>=3.0.0
dynesty>=2.0.0
emcee>=3.0.2
extinction>=0.2.2
h5py>=3.8.0
inflect>=1.0.0
matplotlib>=3.0.0
mpi4py>=4,<5
nbstripout>=0.2.9
numexpr>=2.6.1
numpy>=2,<3
pandas>=2.1
palettable>=2.1.1
schwimmbad>=0.4
scipy>=1.13
seaborn>=0.7.1
six>=1.0.0
